import React from 'react'

const Product = () => {
  return (
    <div>
      <button onClick={() => console.log("Hello")}>
        Click Product
      </button>
    </div>
  )
}

export default Product
